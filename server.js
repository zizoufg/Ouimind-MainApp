const app = require("./backend/app");
const getService = require("./backend/ServiceDiscovery");

// Define an async function to handle starting the server and performing service discovery

    // Start listening for requests
    app.listen(3001, async () => {
        console.log("App is listening on PORT 3001 !!!");

       
});

// Call the async function to start the server and perform service discovery

